import PrimaryForm from '@components/forms/PrimaryForm'
import PrimarySection from '@components/layouts/PrimarySection'

const ContactSection = () => {
  return (
    <PrimarySection sectionText="Contact">
      <PrimaryForm />
    </PrimarySection>
  )
}

export default ContactSection
